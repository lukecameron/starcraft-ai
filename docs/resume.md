# Resume

Checkpoint: 2026-09-20 04:51 UTC. Continue the project brief's autonomous empirical loop; no robust strength incumbent or calibrated local Elo has been established.

- **Reference:** frozen native McRave Zerg port `c49ee3c4b20a2772f1bfca03249c182d32a7238f9453a7a568990a188a1c4c65`, source/build details in `config/baseline.json`. Keep this reference. Canonical local source remains the unadopted coordinate/goal-cache candidate `98df2632…`; experimental forks are isolated and frozen separately.
- **Latest performance result:** `quick-mcrave-as-bounds-v1`, candidate `ddc0415a…`, completed all ten fixed games, 10W/0L, opposing results, no crashes, timeouts or hash mismatches, and 20 archived replays. REJECT against the registered engineering gate: six games below 384 durable fps, aggregate 373.9 fps, minimum 235.3. This is not a causal comparison or strength promotion. See `docs/evaluations/quick-as-bounds-v1/RESULT.md`.
- **Path equivalence:** preceding A* dimension/index shadow passed 13,439 calls with identical reachability, distance bits, paths and callback counts. It exposed 569.6M heuristic and 1.126B walkability callback invocations. `harness_sol` is running the approved per-search memoization shadow as exact exec session46565, one120s trial, frozen module `daeab5c4…`. Root and Luna source review passed; native and all114 official-header TUs compile. Await its durable summary and registered decision. No overlapping games/builds.
- **Stardust:** native port `b6097c9a…` completed the smoke and three of four cohort games, but crashed in the mining forecast in cohort game 1. Cohort REJECT, all original failures retained. A logging/abort diagnostic `c1afd9a5…` confirmed the timer prediction set becomes empty after excluding its only value, zero; it stopped deliberately at frame765 in run `20260920T044040-2281fd96a9f4`. The isolated fallback fork `10399758…` passed its one registered repair probe and all four compatibility games (four wins, aggregate2059.4fps, no hash mismatch or consumer fallback). It is accepted only as a provisional local opponent; config/opponents.json points to its frozen artifact. Preserve original port and diagnostic; no optimizer was disabled. `engine_sol` is now idle. See `docs/evaluations/stardust-cohort-v1/RESULT.md` and diagnostic artifacts.
- **Engine:** canonical timer/lifetime-repaired package `f1a32e5a…` is unchanged. Current experiments use logging-only engine `27ea649c…` in `artifacts/builds/27ea649cb3def9a724df077adaae3e62f16c9de8fbfbc9850ee485af5fbd88cb/openbw-kill-path-diagnostic/` (flat files). Every kill source is logged. Historical reciprocal winners remain unclassified and unscored; two targeted diagnostic attempts and the latest ten-game batch showed ordinary cleanup, not hash mismatch. Do not implement a speculative shutdown barrier.
- **Earlier results:** first c49 quick batch was INCONCLUSIVE (7/10 clean, engine crash, two timeouts). Coordinate/cache batch98df was INCONCLUSIVE (9/10 clean, last game reciprocal winners; aggregate403fps but five below384). Post-cache profile found path generation at least22.08% of samples; goal work0.24%. Source review found shadowed locals in Navigation.cpp but no separate policy experiment has been run.
- **Dashboard:** authorized Pages project `starcraft-ai`, https://starcraft-ai.pages.dev/. Build `scripts/build-pages.sh`, output `public`, root `/`, main. Alternate replay viewer `https://dgant.github.io/openbw-replay-viewer/?url=<encoded-public-replay-url>` is browser-playback verified; direct downloads retained. Our originals/ports/forks carry ownership and author/source links. Local Elo observations show individual builds and experiments, with no inferred BASIL equivalence. Generic runner advice is now separate from reviewed experiment verdicts/conclusions; regression tests and local browser verification passed.
- **Publishing:** completion hook publishes batch artifacts, then rewrites tracked snapshot replay URLs to immutable deployment URLs so clean Git builds retain replay access. Manual publish session88373 completed at https://d1d80202.starcraft-ai.pages.dev with the repaired Stardust cohort and Fork attribution. Prior verdict fix is browser-verified. Last pushed source checkpoint is d99caaf; root is backing up subsequent repair work.
- **Validation:** 31 Python tests passed after exporter/batch-note fix; diff check passed. McRave114, UAlberta115 and Stardust147+16 official-header translation units compile. No original Windows runtime or packaged Win32 DLL proof yet.
- **Quota:** 18% weekly remaining at04:48UTC. An async question is pending for explicit approval to consume one reset credit at <=10%; its expiry is21September10:21Sydney. Do not consume without the user answer. Heavy usage is authorized. Brief asks notification around<15%.
- **Environment:** local Blizzard StarEdit MPQs and 15 SSCAIT maps, native ARM64 OpenBW, four compile jobs maximum. Bulk artifacts/replays/game assets remain ignored and preserved. A local dashboard HTTP server session85590 serves127.0.0.1:8787; no replay playback tab is active.
- **Permissions:** main backups and Pages publishing authorized. No contact, tournament submission or paid services. Stardust derivative tournament submission needs written author permission. Opponents are our native ports and are not calibrated anchors merely because their upstream names have BASIL ratings.

Active source work stays isolated; record raw evidence and fixed acceptance criteria before each trial. Keep failed attempts and avoid replacing seeds. Continue reliability/performance work, then expand the reliable opponent gradient and held-out strength evaluation.
