# McRave large-state goal-cache shadow plan

The first same-state validator observed 5,780 matching calls but never exceeded three eligible units or two requested units. This one-attempt follow-up targets the preserved hotspot trajectory without changing the validator or its correctness threshold.

In isolated diagnostic source, after normal learning selection and immediately before the existing selection log, set the already supported opening to `HatchPool`, `11Hatch`, `3HatchMuta`. The original profile used this opening and ended with 129 zerglings. The override changes the trajectory and may still fail to expose an enemy expansion or produce a qualifying goal call. It is not a strategy proposal and adds no environment or configuration interface.

Run Destination at LF3 with engine and McRave seed 3001, McRave Zerg versus the frozen UAlbertaBot Terran, capped at 180 seconds. Pass requires zero divergence between original shadow, cached shadow, and actual original gameplay, plus at least one call with 20 eligible units and 10 requested units. Run exactly once and retain failure if coverage is insufficient. Instrumentation timing is not production throughput, and no result supports strength or promotion.
